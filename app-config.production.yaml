# Use redirect-based OAuth flow instead of a popup window
# See https://backstage.io/docs/auth/#sign-in-configuration
# Environment variable: OPENCHOREO_FEATURES_AUTH_REDIRECT_FLOW_ENABLED
enableExperimentalRedirectFlow: ${OPENCHOREO_FEATURES_AUTH_REDIRECT_FLOW_ENABLED}

app:
  title: OpenChoreo Portal
  # Should be the same as backend.baseUrl when using the `app-backend` plugin.
  # Environment variable is injected by Helm chart (see https://github.com/openchoreo/openchoreo install/helm/openchoreo/templates/backstage/deployment.yaml)
  # For local k3d: http://openchoreo.localhost (set via https://github.com/openchoreo/openchoreo install/dev/openchoreo-values.yaml)
  # For production: external ingress URL (set via https://github.com/openchoreo/openchoreo install/helm/openchoreo/values.yaml backstage.baseUrl)
  baseUrl: ${BACKSTAGE_BASE_URL}

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  auth:
    dangerouslyDisableDefaultAuthPolicy: true # TODO: Remove this when added auth properly
  # Environment variable is injected by Helm chart (see https://github.com/openchoreo/openchoreo install/helm/openchoreo/templates/backstage/deployment.yaml)
  # For local k3d: http://openchoreo.localhost (set via https://github.com/openchoreo/openchoreo install/dev/openchoreo-values.yaml)
  # For production: external ingress URL (set via https://github.com/openchoreo/openchoreo install/helm/openchoreo/values.yaml backstage.baseUrl)
  baseUrl: ${BACKSTAGE_BASE_URL}
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: ${BACKSTAGE_BASE_URL}
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # Database configuration
  # Supports both SQLite (default) and PostgreSQL via environment variables
  # Set by Helm chart based on backstage.database.type (sqlite or postgresql)
  #
  # For SQLite:
  #   DATABASE_CLIENT=better-sqlite3
  #   SQLITE_STORAGE_DIR=/app/.config/backstage (directory for per-plugin db files)
  #
  # For PostgreSQL:
  #   DATABASE_CLIENT=pg
  #   POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
  database:
    client: ${DATABASE_CLIENT}
    connection:
      # SQLite settings (used when DATABASE_CLIENT=better-sqlite3)
      # Uses directory for per-plugin database files
      directory: ${SQLITE_STORAGE_DIR}
      # PostgreSQL settings (used when DATABASE_CLIENT=pg)
      host: ${POSTGRES_HOST}
      port: ${POSTGRES_PORT}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      database: ${POSTGRES_DB}
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  providers:
    # OpenChoreo Auth - Works with any OAuth2/OIDC-compliant identity provider
    # Environment variables are injected by Helm chart (see https://github.com/openchoreo/openchoreo install/helm/openchoreo/templates/backstage/deployment.yaml)
    #
    # Configuration modes:
    # 1. OIDC Discovery (preferred): Set metadataUrl to auto-discover endpoints
    # 2. Explicit URLs: Set authorizationUrl and tokenUrl directly
    openchoreo-auth:
      development:
        clientId: ${OPENCHOREO_AUTH_CLIENT_ID}
        clientSecret: ${OPENCHOREO_AUTH_CLIENT_SECRET}
        # OIDC Discovery URL (optional)
        metadataUrl: ${OPENCHOREO_AUTH_METADATA_URL}
        # Explicit OAuth2 endpoints
        authorizationUrl: ${OPENCHOREO_AUTH_AUTHORIZATION_URL}
        tokenUrl: ${OPENCHOREO_AUTH_TOKEN_URL}
        scope: ${OPENCHOREO_AUTH_OIDC_SCOPE} # Defaults to 'openid profile email' if unset

    # Guest provider - used when openchoreo.features.auth.enabled is false
    # Allows users to access the portal without authentication (demo/development mode)
    guest:
      dangerouslyAllowOutsideDevelopment: true

catalog:
  locations:
    # Placeholder groups for Backstage entity owner references
    - type: file
      target: /app/catalog-entities/org.yaml
      rules:
        - allow: [Group]
    - type: file
      target: /app/templates/create-openchoreo-project/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-componenttype/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-trait/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-workflow/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-environment/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-namespace/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-deploymentpipeline/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-clustercomponenttype/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-clustertrait/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: /app/templates/create-openchoreo-clusterworkflow/template.yaml
      rules:
        - allow: [Template]

openchoreo:
  # Environment variables are injected by Helm chart (see https://github.com/openchoreo/openchoreo install/helm/openchoreo/templates/backstage/deployment.yaml)
  # OPENCHOREO_API_URL: defaults to internal K8s service URL or set via backstage.openchoreoApi.url
  baseUrl: ${OPENCHOREO_API_URL}

  # Authentication configuration
  # User-initiated requests: Token forwarded from frontend (IDP access token via x-openchoreo-token header)
  # Background tasks (Catalog Provider): Uses client credentials below
  auth:
    # OAuth2 Client Credentials for background tasks (Catalog Entity Provider)
    # Required for the Catalog Provider to fetch organizations, projects, and components
    clientId: ${OPENCHOREO_AUTH_CLIENT_ID}
    clientSecret: ${OPENCHOREO_AUTH_CLIENT_SECRET}
    tokenUrl: ${OPENCHOREO_AUTH_TOKEN_URL}
    scope: ${OPENCHOREO_AUTH_SCOPE} # Optional: space-separated scopes (e.g. 'api://client-id/.default openid')

  # Default owner for built-in Backstage entity kinds (Domain, System, Component, API)
  # Required by Backstage schema validation. Custom OpenChoreo kinds don't use owner.
  defaultOwner: 'openchoreo-users'

  # Periodic full-sync schedule. Acts as a safety net behind the
  # event-driven path; with the event-forwarder disabled, it is the
  # only sync mechanism so you may want a much smaller frequency.
  # Environment variable: OPENCHOREO_CATALOG_SYNC_FREQUENCY (seconds)

  # Upstream URL the openchoreo-perch-backend plugin forwards to (Perch
  # AI assistant). Optional — uncomment only when the assistant feature
  # is enabled (openchoreo.features.assistant.enabled = true) and the
  # perch-agent service is deployed. The perch-backend plugin self-
  # disables when this key is absent, matching the thunder.token pattern.
  # OPENCHOREO_PERCH_AGENT_URL: e.g. http://perch-agent.openchoreo-control-plane.svc.cluster.local:8080
  perchAgentUrl: ${OPENCHOREO_PERCH_AGENT_URL}

  schedule:
    frequency: ${OPENCHOREO_CATALOG_SYNC_FREQUENCY}
    timeout: 120 # seconds for timeout (default: 120)

  # Whether to wire the OpenChoreo event-driven catalog sync. Mirrors
  # the event-forwarder's enabled state — when false, the event
  # router and entity-provider subscriptions are skipped and the
  # system runs in poll-only mode driven by `schedule.frequency`.
  # Environment variable: OPENCHOREO_EVENTS_ENABLED
  events:
    enabled: ${OPENCHOREO_EVENTS_ENABLED}

  # Feature flags for enabling/disabling OpenChoreo functionality
  # Environment variables: OPENCHOREO_FEATURES_WORKFLOWS_ENABLED, OPENCHOREO_FEATURES_OBSERVABILITY_ENABLED, OPENCHOREO_FEATURES_AUTH_ENABLED, OPENCHOREO_FEATURES_AUTHZ_ENABLED, OPENCHOREO_FEATURES_ASSISTANT_ENABLED
  features:
    # Workflow plane / Workflows functionality
    # When disabled, hides Workflows tab and WorkflowsOverviewCard from entity pages
    workflows:
      enabled: ${OPENCHOREO_FEATURES_WORKFLOWS_ENABLED}
    # Observability plane features (Metrics, Traces, Runtime Logs)
    # When disabled, hides Metrics, Traces, Runtime Logs tabs and RuntimeHealthCard from entity pages
    observability:
      enabled: ${OPENCHOREO_FEATURES_OBSERVABILITY_ENABLED}
    # Authentication configuration
    # When disabled (false), users are automatically logged in as guests (no OAuth required)
    auth:
      enabled: ${OPENCHOREO_FEATURES_AUTH_ENABLED}
    # Authorization configuration (Access Control)
    # When disabled, hides Access Control sidebar item and pages
    authz:
      enabled: ${OPENCHOREO_FEATURES_AUTHZ_ENABLED}
    # OpenChoreo Assistant chat agent (FAB, drawer, contextual launchers)
    # Opt-in: defaults to false. Requires the perch-agent service and an
    # LLM API key Secret to be deployed.
    assistant:
      enabled: ${OPENCHOREO_FEATURES_ASSISTANT_ENABLED}
    # Secret Management configuration
    # When disabled, hides the Secrets settings tab and disables secret APIs
    secretManagement:
      enabled: ${OPENCHOREO_FEATURES_SECRET_MANAGEMENT_ENABLED}

thunder:
  # Environment variables are injected by Helm chart (see https://github.com/openchoreo/openchoreo install/helm/openchoreo/templates/backstage/deployment.yaml)
  # THUNDER_BASE_URL: set via backstage.thunder.baseUrl in Helm values (e.g., https://idp.example.com)
  # THUNDER_TOKEN: set via backstage.thunder.token in Helm values (optional, for API authentication)
  baseUrl: ${THUNDER_BASE_URL} # e.g., https://idp.example.com or http://sts.openchoreo.localhost
  # token: ${THUNDER_TOKEN} # Optional: uncomment and set backstage.thunder.token in Helm values if needed
  defaultNamespace: 'default' # Default namespace for User and Group entities
  schedule:
    frequency: 600 # seconds between runs (default: 600 = 10 minutes)
    timeout: 300 # seconds for timeout (default: 300 = 5 minutes)

# =====================================================================
# External CI Platform Configuration
# =====================================================================
# External CI configs are injected via Helm ConfigMap when enabled.
# See: backstage.externalCI.jenkins.enabled, backstage.externalCI.gitlab.enabled
#
# The Helm chart mounts an additional config file (app-config.ci.yaml) when
# CI integrations are enabled. This avoids startup errors when configs reference
# unset environment variables.
#
# Entity annotations control which CI platform is shown for each component:
#   - Jenkins:        jenkins.io/job-full-name
#   - GitHub Actions: github.com/project-slug (uses integrations.github token)
#   - GitLab:         gitlab.com/project-slug or gitlab.com/project-id
# =====================================================================

# Event-driven catalog sync — accepts webhook POSTs from the OpenChoreo
# event-forwarder at /api/events/http/openchoreo. The Dockerfile copies
# this file over the base app-config.yaml during image build, so this
# section needs to live here (not just in the source app-config.yaml)
# for the in-cluster Backstage to register the ingress route.
events:
  http:
    topics:
      - openchoreo

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: ${OPENCHOREO_FEATURES_AUTHZ_ENABLED}
